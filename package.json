{
  "name": "ts-midi-sequencer",
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc && vite build",
    "preview": "vite preview",
    "validate:schema": "node scripts/validate-schemas.js"
  },
  "devDependencies": {
    "typescript": "^5.3.0",
    "vite": "^5.0.0",
    "ajv": "^8.12.0"
  }
}
